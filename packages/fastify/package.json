{
  "name": "@hulla/api-fastify",
  "version": "2.0.0",
  "description": "Fastify server adapter for @hulla/api implementations",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown",
    "clean": "rm -rf dist coverage",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hulla/api-node": "workspace:^"
  },
  "devDependencies": {
    "@hulla/api": "workspace:*",
    "fastify": "^5.12.1",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "@hulla/api": "^2.0.0",
    "fastify": "^5.12.1"
  }
}
